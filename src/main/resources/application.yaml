spring:
  application:
    name: task-api

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:postgresql://localhost:5432/task_api
    username: postgres
    password: postgres

server:
  port: 8080
